# Reddit Scraping (RedditDB)
praw>=7.7.0
prawcore>=2.3.0

# Web Scraping (Beans)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# LLM Integration (Beans + Data Analysis)
ollama>=0.1.0
pydantic>=2.0.0

# Data Analysis - NLP & ML
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
pyspellchecker>=0.7.0
bertopic>=0.15.0

# Data Analysis - Text Processing
language-tool-python>=2.7.0

# Data Analysis - Database & Data Processing
psycopg2-binary>=2.9.0
pandas>=2.0.0
sqlalchemy>=2.0.0

# Optional dependencies (uncomment if needed)
# mlx-lm>=0.1.0  # For Apple Silicon LLM inference (macOS only)

# Note: Most other requirements are Python standard library
# (json, re, pathlib, configparser, signal, datetime, etc.)
